enum FocusMode { short, deep, flow }

enum MoodState { sad, bored, neutral, happy, ecstatic }

enum TrustLevel { stranger, friend, partner, family, soulBond }

enum Rarity { common, rare, epic, legend }

enum RatAnimationState {
  idle,
  studying,
  eating,
  confused,
  sad,
  happy,
  sleeping,
  walking,
}
